<Window x:Class="SpotifyRemote.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="http://www.hardcodet.net/taskbar" 
        xmlns:conv="clr-namespace:SpotifyRemote.App.Converters"
        mc:Ignorable="d"
        Title="Spotify Remote Server" Height="450" Width="400"
        Background="#181818" Foreground="White"
        ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <conv:BooleanToStringConverter x:Key="BooleanToStringConverter"/>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#282828"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B3B3B3"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1DB954"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- System Tray Icon -->
    <Grid>
        <tb:TaskbarIcon x:Name="MyNotifyIcon"
                        ToolTipText="Spotify Remote Server"
                        MenuActivation="RightClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open Settings" Click="ShowWindow_Click"/>
                    <Separator/>
                    <MenuItem Header="Exit" Click="Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <StackPanel Margin="20">
            <TextBlock Text="Spotify Remote" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <TextBlock Text="Target Device (Bluetooth Name)"/>
            <TextBox Text="{Binding DeviceName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Server Port"/>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Spotify Playlist URI"/>
            <TextBox Text="{Binding SpotifyUri, UpdateSourceTrigger=PropertyChanged}"/>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="{Binding IsServerRunning, Converter={StaticResource BooleanToStringConverter}, FallbackValue='Toggle Server'}" 
                        Command="{Binding ToggleServerCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsServerRunning}" Value="True">
                                    <Setter Property="Background" Value="#E91E63"/> 
                                    <Setter Property="Content" Value="Stop Server"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsServerRunning}" Value="False">
                                    <Setter Property="Content" Value="Start Server"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <Button Grid.Column="2" Content="Test Trigger" Background="#404040" Command="{Binding TestTriggerCommand}"/>
            </Grid>

            <TextBlock Text="Status Log" Margin="0,20,0,5" FontWeight="Bold"/>
            <Border Background="#121212" CornerRadius="5" Padding="10" Height="80">
                <TextBlock Text="{Binding StatusLog}" TextWrapping="Wrap" Foreground="#00E676"/>
            </Border>
            
            <TextBlock Text="Close window to minimize to tray." HorizontalAlignment="Center" Margin="0,10" FontSize="12" Opacity="0.6"/>
        </StackPanel>
    </Grid>
</Window>
